# Human-readable top-level dependencies. The authoritative, reproducible pin
# set is requirements.lock (hash-locked, generated by uv from pyproject.toml;
# see "Air-gapped / reproducible install" in the README for the workflow and
# the refresh policy).
numpy
pandas
scipy
scikit-learn
xgboost
sentence-transformers
